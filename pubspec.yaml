name: choperia_820_app
description: App mobile da Choperia 820
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.9
  provider: ^6.1.2
  google_fonts: ^6.2.1
  http: ^1.2.2
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/produtos/
